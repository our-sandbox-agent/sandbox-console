# 開發規範

每條規則都來自這個專案實際發生過的事，不是通用樣板。改規則請一併說明是哪次經驗讓它需要改。

## 分支與 PR

- **一律以 `main` 為基底**。要疊在另一個未合併的 PR 上時，在說明寫清楚依賴，並等前一個合併後 rebase 回 `main`。
  *為什麼*：#57 的 base 設成了部落格分支，合併後功能只存在於那條分支，`main` 一直沒有，#41 也關不掉，最後要另外 cherry-pick 救回（#64）。
- **一個 PR 一件事**。功能、重構、格式整理分開送。
  *為什麼*：#54 同時夾帶部落格與一個已完成的產品功能，導致「部落格要等品牌定案」把產品功能一起凍住。
- **規格 PR 不用關票關鍵字**，只寫 `Refs #<n>`。只有附上實測證據的 PR 才能用 `Closes`。
  *為什麼*：#8 曾經在只有預檢工具、沒有任何 Linux 實測結果時被關掉。
- **PR 說明要寫「怎麼驗證的」**：跑了哪些測試、實際數字、以及哪些部分沒驗到。沒驗到的比驗到的更需要寫。

## 測試

- **純邏輯模組必須有測試**。不依賴 DOM、網路或 Docker 的判斷邏輯要抽成獨立模組，用 `node:test` 或 `unittest` 覆蓋。
  *慣例出處*：`lifecycle.js`、`file-deletion.js`、`file-upload.js` 都是這樣做的；`blog.js` 沒有，所以另開了 #56。
- **把關條件要被測試守住**。凡是決定 pass／fail、允許／拒絕、可合併／不可合併的函式，每一個條件都要有一個會失敗的測試。做法是把該條件改成永真，測試必須紅。
  *為什麼*：`case_passed` 有九個條件，原本只有兩個被守住，刪掉其中七個 CI 照樣全綠（#65 已補齊）。
- **沒有測試的腳本至少要進 CI 的語法檢查**。`python3 -m compileall -q scripts diagnostics` 已在 CI，新增目錄要一併加入。
- **修 bug 先寫會失敗的測試**，再修。

## 證據與秘密

- **任何憑證都不進 repo**：不寫進程式、issue、PR、測試資料或證據包。
- **證據包一律經過遮蔽**。#8 的所有寫入都走 `Matrix.write()` 與 `Matrix.log()`，遮蔽掛在那裡。不符合已知格式的憑證要自己登記：`--redact-file <私密檔案>`，不要用命令列參數，那會留在 shell history。
- **送出前自己 grep 一次**已知的秘密值，確認沒有命中。遮蔽是降低意外外洩，不是保證。
- **實測證據要能回溯**：記錄環境版本、image digest、起迄時間、原始 exit code 與量測值。沒觀測到的東西不要寫成觀測到。
  *為什麼*：E08 的取樣沒抓到 OOM 計數器，文件如實寫明並改用其他證據推論，這是正確做法。
- **清理只刪自己建立的資源**，先驗 label 再刪，不用 `prune` 這類全域指令。

## 內容即程式碼

- **`.mdx`、`.html`、模板字串裡的內容以程式碼標準審查**。MDX 會在頁面載入時求值，跟使用者資料同源。
- **文件裡的費率、版本、外部狀態要附來源與查證日期**。這些會過期。
  *為什麼*：競品費率、上游 issue 狀態、gVisor release 版本在這個專案裡都被引用過，其中一部分已經改變。
- **對外文案不能承諾雛形沒做到的事**。願景要標成願景，並在同一畫面說明目前的限制。

## 重構與死碼

- **定期整理**：每個里程碑結束或每 4 週，以先到者為準，開一個只做整理的 PR。刪掉沒有呼叫者的函式、檔案、設定、CI 步驟與過期文件，合併重複邏輯。不改行為，附測試或驗收證據證明行為不變，說明裡列出刪除清單。
- **功能 PR 順手清理**自己改動範圍內的死碼，但不把大範圍重構夾帶進來。
- **工具**：JavaScript 與 TypeScript 用 `npx knip`；Go 用 `go run golang.org/x/tools/cmd/deadcode@latest ./...` 與 `staticcheck`；Python 用 `vulture`。結果先人工確認再刪。

## 決策紀錄

- **架構與技術路線的決定寫成 ADR**，放 `docs/adr/`，狀態用 Proposed 或 Accepted，Accepted 要記決策人與日期。
- **不要把「之後再重寫」排成里程碑必交項目**。語言、框架、底層一次選定。
  *為什麼*：原計劃排了「先用 Node 寫 Runner，第 3 步改寫成 Go」，這種暫時版很容易變成永久版（見 `docs/adr/runner-language.md`）。
- **決定改變時，同一個 PR 要把受影響的文件一起改**，不要留下互相矛盾的兩份說法。
