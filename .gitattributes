# Evidence hashes must survive Windows checkouts without newline conversion.
docs/research/evidence/** -text
# Preserve whitespace in raw build output rather than editing observed bytes.
docs/research/evidence/**/build.log -whitespace
docs/research/evidence/**/runtime-logs/** -whitespace
